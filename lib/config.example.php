<?php
/**
 * Hypohub — exemple de configuration.
 *
 * Ce fichier est le modèle. Ne le modifie pas.
 * L'assistant d'installation (config.php à la racine) crée « config.local.php »
 * à partir de tes réponses, dans ce même dossier lib/.
 *
 * Si tu préfères configurer à la main : copie ce fichier en « config.local.php »
 * et remplis les 4 valeurs ci-dessous.
 */
return array(
    // Adresse du serveur MySQL (souvent « localhost » sur un hébergement)
    'db_host' => 'localhost',
    // Nom de la base de données que tu as créée dans le panneau
    'db_name' => 'hypohub',
    // Usager MySQL de la base
    'db_user' => 'root',
    // Mot de passe MySQL de la base
    'db_pass' => '',
);
